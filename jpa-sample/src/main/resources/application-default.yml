# config for dataSource
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: null
    driver-class-name: org.h2.Driver
    initial-size: 1
    maximum-pool-size: 1
